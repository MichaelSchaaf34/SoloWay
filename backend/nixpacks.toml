# Nixpacks configuration for Railway deployment
# Alternative to Dockerfile for simpler deployments

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --only=production"]

[phases.build]
cmds = []

[start]
cmd = "node src/index.js"

[variables]
NODE_ENV = "production"
